% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{cli.install}
\alias{cli.install}
\title{Install the artma CLI launcher}
\usage{
cli.install(dir = "~/.local/bin", force = FALSE)
}
\arguments{
\item{dir}{\emph{[character, optional]} Directory to install the launcher into.
Defaults to \code{"~/.local/bin"}.}

\item{force}{\emph{[logical, optional]} Required to install in a non-interactive
session. Ignored (the menu governs) when interactive. Defaults to \code{FALSE}.}
}
\value{
\emph{[character]} Path to the installed launcher (invisibly).
}
\description{
Copy the launcher shipped inside the installed package to a directory on your
\code{PATH}, so \verb{artma <command>} works from any shell. The launcher relies solely
on the installed package; it resolves nothing from its own location.

The no-install alternative is to call the CLI through \code{Rscript} directly:
\verb{Rscript -e 'artma::cli.run()' run --options my_analysis.yaml}.

In interactive sessions the copy is confirmed via a menu. In non-interactive
sessions it requires \code{force = TRUE} and otherwise aborts. It never writes
inside \code{R.home()} and never elevates privileges.
}
\examples{
\dontrun{
artma::cli.install()
artma::cli.install(dir = "~/bin", force = TRUE)
}
}
