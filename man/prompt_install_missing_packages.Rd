% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artma.R
\name{prompt_install_missing_packages}
\alias{prompt_install_missing_packages}
\title{Offer to install missing suggested packages}
\usage{
prompt_install_missing_packages(
  pkgs,
  method_name,
  is_installed = NULL,
  install_packages = NULL
)
}
\arguments{
\item{pkgs}{\emph{[character]} Packages the method suggests but that are absent.}

\item{method_name}{\emph{[character]} Method the packages are needed for.}

\item{is_installed}{\emph{[function, optional]} Predicate testing package
availability. Injectable for testing; defaults to \code{requireNamespace}.}

\item{install_packages}{\emph{[function, optional]} Installer. Injectable for
testing; defaults to \code{utils::install.packages}.}
}
\value{
\emph{[character]} The packages that remain missing.
}
\description{
In interactive sessions, offer to install the optional packages a method
needs before it runs. Returns the packages still missing afterwards (the
whole set unchanged when the user declines or the session is
non-interactive), so the caller can decide to skip the method.
}
\keyword{internal}
