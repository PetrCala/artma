% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{cli.run}
\alias{cli.run}
\title{Run the artma command-line interface}
\usage{
cli.run(args = commandArgs(trailingOnly = TRUE))
}
\arguments{
\item{args}{\emph{[character, optional]} The argument vector to parse. Defaults
to \code{commandArgs(trailingOnly = TRUE)}.}
}
\value{
\emph{[integer]} The exit code (invisibly).
}
\description{
Scriptable entry point behind the \code{artma} launcher and
\verb{Rscript -e 'artma::cli.run()'}. It parses an argument vector, dispatches to
the matching public function (\code{artma()}, \code{methods.list()},
\code{options.validate()} / \code{options.create()} / \code{options.list()}, or the package
version) and returns an exit code. No analysis logic lives here: each
subcommand is a thin translation to the existing API.

The function never calls \code{quit()}; it returns the exit code invisibly so it
can be driven in-process by tests. The launcher script turns that code into a
process exit status.

Exit codes:
\itemize{
\item \code{0} success (including \code{--help}).
\item \code{1} an R error was raised while dispatching (message printed to stderr).
\item \code{2} a usage error (unknown command, unknown flag, malformed value); usage
is printed to stderr.
}

Subcommands: \code{run}, \code{methods}, \code{options} (with sub-actions \code{validate},
\code{create}, \code{list}) and \code{version}. Every subcommand accepts \code{--help}.

Flags for \code{run} become an in-session \code{options()} overlay applied around the
\code{artma()} call, so the user's YAML options file is never mutated:
\code{--data} sets \code{artma.data.source_path}, \code{--output-dir} sets
\code{artma.output.dir}, \code{--verbose} sets \code{artma.verbose}, \code{--no-cache} sets
\code{artma.cache.use_cache} to \code{FALSE}, and \code{--report} sets
\code{artma.output.report} to \code{TRUE}. \code{--options}, \code{--options-dir} and
\code{--methods} are forwarded as \code{artma()} arguments.

In \code{--json} mode stdout carries only a JSON run manifest (\code{methods_run},
\code{methods_skipped} with reasons, \code{output_dir}, \code{exported_files}, \code{seed},
\code{package_version}), read back from the \code{run.json} the run wrote; all
human-readable output is routed to stderr.
}
\examples{
\dontrun{
# List available methods
artma::cli.run("methods")

# Run two methods against an options file, emitting a JSON manifest
artma::cli.run(c(
  "run", "--options", "my_analysis.yaml",
  "--methods", "funnel_plot,effect_summary_stats", "--json"
))
}
}
