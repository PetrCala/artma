% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artma.R
\name{execute_run}
\alias{execute_run}
\title{Run the methods of a prepared run}
\usage{
execute_run(context, methods = NULL, ...)
}
\arguments{
\item{context}{\emph{[list]} The run context returned by \code{prepare_run_context()}.}

\item{methods}{\emph{[character, optional]} A character vector of the methods to
invoke. \code{NULL} prompts for a selection.}

\item{...}{\emph{[any]} Additional arguments passed to the runtime methods.}
}
\value{
\emph{[list]} A list with \code{results} (the invocation results, carrying the
\code{run_info}, \code{failed_methods} and \code{skipped_methods} attributes), \code{run_files}
(the files this run wrote; empty when results are not saved) and \code{context}
(the run context, with \code{output_dir} set to the directory this run used).
}
\description{
Second step of the \code{artma()} pipeline: invoke the runtime
methods on the prepared data frame and, when the run saves results, export
the tables, optionally render the HTML report, and write the run manifest.

The output-file capture opened by \code{prepare_run_context()} is closed here,
after the export and the report, so the manifest lists the exported tables
and the report alongside the graphics the methods wrote.
}
\keyword{internal}
