% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{options_list}
\alias{options_list}
\title{List available user options}
\usage{
options_list(
  options_dir = NULL,
  should_return_verbose_names = FALSE,
  details = FALSE,
  template_path = NULL
)
}
\arguments{
\item{options_dir}{\emph{[character, optional]} Full path to the folder that contains user options files. If not provided, the default folder is chosen. Defaults to \code{NULL}.}

\item{should_return_verbose_names}{\emph{[logical, optional]} If set to TRUE, the custom names of each of the options files are read and returned instead of file names. Defaults to FALSE.}

\item{details}{\emph{[logical, optional]} If set to TRUE, a \code{data.frame} describing each options file is returned instead of a character vector: the file name, its resolved \code{data.source_path}, when the file was last modified, when it was last run, and how many of its options deviate from the template defaults. The last run time is read from the file's output directory, and is \code{NA} when the file has never produced any results. Defaults to FALSE.}

\item{template_path}{\emph{[character, optional]} Full path to the options template file, used to resolve the defaults when \code{details = TRUE}. Defaults to \code{NULL}.}
}
\value{
\emph{[vector, character | data.frame]} A character vector with the names of the options available, or a \code{data.frame} when \code{details = TRUE}.
}
\description{
Retrieves the list of the existing options files and returns their names as a character vector. By default, this retrieves the names of the files including the yaml suffix, but can be modified to retrieve options verbose names instead.
}
