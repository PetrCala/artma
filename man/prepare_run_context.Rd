% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artma.R
\name{prepare_run_context}
\alias{prepare_run_context}
\title{Prepare a run}
\usage{
prepare_run_context(data = NULL, methods = NULL)
}
\arguments{
\item{data}{\emph{[data.frame, optional]} Data frame to analyze. When \code{NULL}, the
data is loaded from the options file.}

\item{methods}{\emph{[character, optional]} The methods the run will invoke, used
to decide which columns the data preparation must resolve.}
}
\value{
\emph{[list]} The run context: \code{df} (the prepared data frame),
\code{output_dir} (the base output directory; \code{NULL} when results are not
saved, and replaced by the run's own directory in \code{execute_run()}),
\code{save_results} and \code{capture} (the open output-file capture frame
identifier).
}
\description{
First step of the \code{artma()} pipeline: resolve the output
directory, open the run's output-file capture, and produce the prepared
data frame. Either loads the data through \code{prepare_data()} or, when a data
frame is supplied, mirrors that pipeline's preprocess and compute phases on
it.

The returned context is what the run and summarize steps consume. The
capture frame it opens stays open on success: the caller owns it and must
close it (the run step does so when it writes the manifest, and \code{artma()}
registers an \code{on.exit()} fallback). A failure inside this function closes the
frame before propagating, so an aborted preparation leaves no dangling frame.

Callable repeatedly within one \code{runtime_setup()} extent, which is what the
session hub needs to re-prepare data from its menu loop.
}
\keyword{internal}
