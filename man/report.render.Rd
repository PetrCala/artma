% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report.render}
\alias{report.render}
\title{Render an HTML report of a run's results}
\usage{
report.render(results, output_file = NULL, open = interactive())
}
\arguments{
\item{results}{\emph{[list]} The named list returned by \code{\link[=artma]{artma()}}, one
\code{new_method_result} per method.}

\item{output_file}{\emph{[character, optional]} Absolute path of the HTML file to
write. When \code{NULL} (default), the report is written as \code{report.html} inside
the most recent run's resolved output directory.}

\item{open}{\emph{[logical, optional]} Whether to open the report in a browser
after writing it. Only ever honoured in an interactive session. Defaults to
\code{interactive()}.}
}
\value{
\emph{[character]} The path of the written report file (invisibly).
}
\description{
Turn the results of an \code{artma()} run into a single self-contained HTML file:
a metadata header, a table of contents, and one section per method with its
tables and plots. The file has no external dependencies (all styling is
inline and every plot is embedded as a base64 PNG), so it can be shared on
its own.

The report can also be produced automatically at the end of every run by
setting the \code{output.report} option to \code{TRUE}.
}
\examples{
\dontrun{
res <- artma(methods = c("funnel_plot", "effect_summary_stats"), options = "my.yaml")
report.render(res)

# Write to a specific location and open it
report.render(res, output_file = "~/analysis/report.html", open = TRUE)
}
}
