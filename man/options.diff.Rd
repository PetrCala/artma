% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{options.diff}
\alias{options.diff}
\title{Diff two user options files}
\usage{
options.diff(
  options_file_name_a = NULL,
  options_file_name_b = NULL,
  options_dir = NULL,
  template_path = NULL
)
}
\arguments{
\item{options_file_name_a}{\emph{[character, optional]} Name of the first options file to compare. If not provided, the user will be prompted. Defaults to \code{NULL}.}

\item{options_file_name_b}{\emph{[character, optional]} Name of the second options file to compare. If not provided, the user will be prompted. Defaults to \code{NULL}.}

\item{options_dir}{\emph{[character, optional]} Full path to the folder that contains user options files. If not provided, the default folder is chosen. Defaults to \code{NULL}.}

\item{template_path}{\emph{[character, optional]} Full path to the options template file. Defaults to \code{NULL}.}
}
\value{
\emph{[list]} Invisibly, a list with the compared \code{files}, a \code{differences} data frame, and a \code{deviations} list holding one data frame of template-default deviations per file.
}
\description{
Compares two user options files, printing the options whose
values differ between them, followed by each file's deviations from the
template defaults.
}
\details{
List-typed options (such as \code{data.columns}) are compared entry by
entry, so the diff names the individual column mappings that differ rather
than reporting the whole store as changed.
}
