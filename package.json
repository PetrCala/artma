{
  "name": "artma-ci-tooling",
  "private": true,
  "description": "Dev-only JS tooling for CI (commitlint). Not part of the R package.",
  "devDependencies": {
    "@commitlint/config-conventional": "21.2.2",
    "commitlint": "21.2.2"
  }
}
