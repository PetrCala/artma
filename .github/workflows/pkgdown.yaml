# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
#
# Builds the pkgdown site and publishes it to the gh-pages branch, which
# GitHub Pages serves at https://petrcala.github.io/artma/. That URL is listed
# in DESCRIPTION and CRAN checks every URL there, so a broken deploy here is a
# submission blocker, not a docs nuisance: it is what got 0.4.0 returned.
name: pkgdown

on:
  push:
    branches: [master]
  release:
    types: [published]
  workflow_dispatch:

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pkgdown:
    runs-on: ubuntu-latest

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Setup JAGS
        uses: ./.github/actions/setup-jags

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown, local::.
          needs: website

      - name: Build site
        run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
        shell: Rscript {0}

      - name: Deploy to GitHub pages
        uses: JamesIves/github-pages-deploy-action@v4.9.0
        with:
          clean: false
          branch: gh-pages
          folder: docs
